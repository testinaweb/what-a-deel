const admin = require('./admin.controller');
const balances = require('./balances.controller');
const contracts = require('./contracts.controller');
const jobs = require('./jobs.controller');

module.exports = { admin, balances, contracts, jobs };
